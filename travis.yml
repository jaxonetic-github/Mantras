language: node_js 
node_js:
 - 14.12.0
 before_install:
- npm install -g codecov

install:
- pip install coverage
- pip install -r requirements.txt

script:
 - coverage run -m unittest Mantras

after_success:
 bash <(curl -s https://codecov.io/bash)